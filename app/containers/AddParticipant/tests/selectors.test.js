// import { selectAddParticipantDomain } from '../selectors';

describe('selectAddParticipantDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
