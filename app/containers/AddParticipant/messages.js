/*
 * AddParticipant Messages
 *
 * This contains all the text for the AddParticipant container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.AddParticipant';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AddParticipant container!',
  },
});
