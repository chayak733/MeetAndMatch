// import { selectUpdateParticipantDomain } from '../selectors';

describe('selectUpdateParticipantDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
