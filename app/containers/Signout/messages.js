/*
 * Signout Messages
 *
 * This contains all the text for the Signout container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.Signout';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Signout container!',
  },
});
